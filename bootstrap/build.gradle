buildscript {
    repositories {
        maven { url "http://repo.spring.io/libs-snapshot" }
        maven { url "http://repo.spring.io/milestone" }
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:1.0.1.RELEASE")
    }
}

apply plugin: 'spring-boot'

jar {
    manifest {
        attributes 'Implementation-Title': 'Mercuree Bootstrap', 'Implementation-Version': version
    }
}

dependencies {
    compile project(':transformations')

    compile("org.projectreactor:reactor-spring:1.0.0.RELEASE")
    compile("org.springframework.boot:spring-boot-starter-jdbc:1.0.1.RELEASE")
    compile("org.springframework.boot:spring-boot-starter-web:1.0.1.RELEASE")
    compile("org.springframework.boot:spring-boot-starter-actuator:1.0.1.RELEASE")
    compile("org.thymeleaf:thymeleaf-spring4:2.1.2.RELEASE")
    compile("org.yaml:snakeyaml:1.13")
    compile("com.fasterxml.jackson.core:jackson-databind")
}

test { systemProperties 'property': 'value' }
